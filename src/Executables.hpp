#pragma once

#include <list>
#include <string>

class Executables {

    std::list<std::string> entries_;

public:

    explicit inline Executables(const std::initializer_list<std::string> &/*list*/) {}

};

